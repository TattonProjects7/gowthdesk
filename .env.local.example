# All optional — Shortlist runs fully with ZERO config (simulated market + rules-based thesis).
# Add any of these to upgrade the demo to real data / AI.

# Real-time quotes (finnhub.io — free tier works)
FINNHUB_API_KEY=

# Daily candle history (alphavantage.co — free tier works)
ALPHAVANTAGE_API_KEY=

# AI bear/bull thesis (falls back to a deterministic generator if unset)
OPENAI_API_KEY=
